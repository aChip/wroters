<h1>新建相册</h1>
 
<% form_for @album, :html => { :multipart => true } do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <p><%= f.submit "创建",:class=>"formbutton" %></p>
<% end %>
 
<%= link_to '返回', albums_path %>

